Before compiling SDL2 it is necessary to have the necessary libraries and headers installed:

$ apt-get install libvorbis-dev libsmpeg-dev libasound2-dev libpulse-dev libpng-dev

Then

$ cd SDL2
$ ./updatesdl.sh

To test main.c

$ make && ./glapp

